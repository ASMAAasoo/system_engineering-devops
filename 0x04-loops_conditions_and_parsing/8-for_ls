#!/usr/bin/env bash
# This script displays the content of the current directory, showing only the part of the filename after the first dash (-)
for file in *-*  # This matches all files containing at least one dash (-)
do
  # Extract and display the part of the filename after the first dash using parameter expansion
  echo "${file#*-}"
done
