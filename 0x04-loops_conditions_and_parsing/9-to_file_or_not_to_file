#!/usr/bin/env bash
# This script lists non-hidden files in the current directory using a for loop,
# and displays only the part after the first dash in each filename

for file in *; do
  # Only process files that contain a dash
  if [[ "$file" == *-* ]]; then
    echo "${file#*-}"
  fi
done
